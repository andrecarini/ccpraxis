{
  "name": "sandbox",
  "version": "0.1.0",
  "description": "Prepare a project for running Claude Code in an isolated Docker-or-Podman (auto-detected) dev container. Provides /sandbox:setup (host-side redirect skill) and /sandbox:test (run the verification suite); the canonical entry point is the claude-sandbox launcher which auto-prompts for bootstrap on first run.",
  "author": {
    "name": "ccpraxis"
  }
}
