#!/usr/bin/env perl
# bp-feedback.pl - capture a single piece of operator feedback into the
# current (or a specified) blueprint's target batch as feedback-<n>.txt,
# with a provenance header, without ever overwriting or losing input.
use strict;
use warnings;

exit 2;
